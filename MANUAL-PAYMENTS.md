# پرداخت دستی و موقعیت تحویل

- هر دو فروشگاه از `CheckoutClient`، دفترچه آدرس و سرویس محاسبه ارسال یکسان استفاده می‌کنند. هزینه و مبلغ نهایی در سرور محاسبه می‌شود.
- در `/admin/settings` سه حساب را تنظیم کنید. نمونه‌های اولیه عمداً شماره‌های غیرقابل واریز دارند و غیرفعال‌اند. فعال‌سازی نیازمند شماره کارت و شبای معتبر است؛ مالکیت حساب باید توسط مدیر بررسی شود.
- سفارش ابتدا `awaiting_receipt` است. توضیحات زمان ثبت سفارش رسید محسوب نمی‌شود. پس از ارسال تصویر یا متن رسید، وضعیت `payment_under_review` می‌شود.
- در جزئیات سفارش ادمین، واریز را با حساب بانکی تطبیق دهید. تأیید نیازمند تاریخ و ساعت تقریبی ارسال (به وقت تهران) است. رد رسید نیازمند دلیل است و امکان ارسال دوباره را باز می‌کند؛ سوابق حفظ می‌شوند.
- تصاویر رسید با Sharp بازخوانی و به WebP تبدیل می‌شوند (حداکثر ورودی ۵ مگابایت و ۲۰ میلیون پیکسل). فایل‌ها در `private-receipts` کنار فایل سفارش‌ها ذخیره می‌شوند؛ هیچ URL عمومی برای رسید ساخته نمی‌شود. دریافت تصویر نیازمند نشست مالک سفارش یا ادمین است.

## پیامک

در تأیید رسید، ادمین تاریخ شمسی و ساعت تهران را انتخاب می‌کند. گزینه ارسال پیامک تأیید پرداخت و زمان تقریبی به‌صورت پیش‌فرض فعال است؛ با برداشتن تیک، پیام در انتظار ارسال می‌ماند و دکمه ارسال در همین صفحه در دسترس است. پیام‌های موفق دوباره ارسال نمی‌شوند.

## دریافت حضوری و ثبت تحویل

گزینه دریافت حضوری در تسویه‌حساب هر دو فروشگاه، نشانی و تلفن مغازه را نمایش می‌دهد و فقط نام و شماره گیرنده را می‌خواهد. نشانی مغازه از تنظیمات سرور روی سفارش ثبت می‌شود؛ آدرس پستی و مختصات مشتری لازم نیست. مبلغ تحویل از تنظیمات روش ارسال محاسبه می‌شود (پیش‌فرض رایگان).

پس از تأیید پرداخت، ادمین آماده‌سازی را شروع می‌کند. سفارش حضوری به «آماده دریافت» و سفارش ارسالی به «ارسال‌شده» می‌رود. ثبت نهایی تحویل نیازمند تیک تأیید تحویل به مشتری است؛ زمان آن در تاریخچه و پنل مشتری ذخیره و نمایش داده می‌شود. درخواست تکراری وضعیت، رویداد یا زمان تحویل جدید نمی‌سازد.

پیامک ثبت رسید و تأیید پرداخت به شماره تأییدشده حساب مشتری ارسال می‌شود، نه شماره قابل ویرایش گیرنده. مسیر پیامک سفارش از OTP جداست و از متغیرهای سرور `SMS_PROVIDER=melipayamak`، `MELIPAYAMAK_USERNAME`، `MELIPAYAMAK_API_KEY` (یا `MELIPAYAMAK_PASSWORD`) و `MELIPAYAMAK_FROM` استفاده می‌کند. اولویت `.env.local` نسبت به `.env` را رعایت کنید. سرویس OTP و تنظیمات پترن آن تغییر نمی‌کند.

پاسخ موفق سرویس فقط پذیرش پیام است، نه تضمین تحویل به گوشی. در حالت `mock` و محیط غیر production، وضعیت پیامک صریحاً آزمایشی است. خطای پیامک وضعیت پرداخت را برنمی‌گرداند؛ ادمین می‌تواند پیامک ناموفق را دوباره ارسال کند. اگر فرآیند حین ارسال متوقف شد و وضعیت `sending` ماند، قبل از هر ارسال دستی پنل پیامک بررسی شود تا پیام تکراری ارسال نشود.

مرجع ارسال: [SDK رسمی ملی‌پیامک](https://github.com/Melipayamak/melipayamak-python/blob/master/melipayamak/sms/rest.py).

## نقشه

Leaflet و کاشی‌های OpenStreetMap، انتخاب با کلیک/کشیدن نشانگر، موقعیت فعلی مرورگر و ورود دستی مختصات را ارائه می‌کنند. موقعیت فعلی روی HTTPS یا localhost و با اجازه کاربر کار می‌کند. جست‌وجوی فارسی فقط با اقدام کاربر، محدود به ایران، از Nominatim انجام می‌شود؛ از autocomplete استفاده نشده است. نام مکان برای جست‌وجو به این سرویس ارسال می‌شود، نه مشخصات گیرنده و نشانی کامل فرم.

برای تغییر ارائه‌دهنده سازگار با Nominatim، `GEOCODING_SEARCH_URL` را به endpoint HTTPS سرور خود تنظیم کنید. در نسخه تک‌فرآیندی فعلی، درخواست‌ها حداکثر یکی در ۱٫۱ ثانیه و نتایج دارای کش محدود هستند. دسترسی واقعی سرویس نقشه به شبکه و ارائه‌دهنده وابسته است؛ ورود دستی مختصات در حالت قطعی در دسترس است.

مراجع: [Leaflet](https://leafletjs.com/reference.html)، [جست‌وجوی Nominatim](https://nominatim.org/release-docs/latest/api/Search/)، [سیاست استفاده](https://operations.osmfoundation.org/policies/nominatim/).

## استقرار و آزمون

این تغییر از ذخیره‌ساز فایل موجود سفارش‌ها استفاده می‌کند؛ تنظیم MongoDB به‌تنهایی آن را به MongoDB منتقل نمی‌کند. `UFO_MOCK_DATA_DIR` باید روی دیسک پایدار و خارج از `public` باشد و همراه تصاویر رسید پشتیبان‌گیری شود. پیش از اجرای چند replica یا چند فرآیند، تراکنش سفارش/رسید و صف پیامک و محدودکننده مشترک جست‌وجو باید به ذخیره‌ساز مشترک منتقل شوند؛ فایل محلی برای این معماری مناسب نیست.

سفارش‌های قدیمی که بدون رسید در وضعیت بررسی مانده‌اند هنگام خواندن به انتظار رسید برگردانده می‌شوند؛ پرداخت‌های تأییدشده قبلی تغییر نمی‌کنند.

تست‌ها: `npm exec -- vitest run tests/integration/manual-payment.test.ts` و `npm exec -- playwright test --config playwright.payment.config.ts`. تست مرورگر درخواست‌های تغییردهنده را با داده آزمایشی پاسخ می‌دهد و سفارش یا پیامک واقعی ایجاد نمی‌کند.
